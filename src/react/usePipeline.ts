import { useState, useEffect, useCallback, useRef } from "react";

/**
 * @returns Object containing the pipeline, loading state, error, and run function
 */
export function usePipeline<T = any>() {
  return {
    test: "test",
  };
}
